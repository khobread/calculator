{
  "name": "calculatorserverside",
  "version": "1.0.0",
  "description": "Calculator that runs computation server side and returns the result to client",
  "main": "calculator.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Kevin Lee",
  "license": "ISC",
  "dependencies": {
    "body-parser": "^1.19.0",
    "express": "^4.17.1"
  }
}
